# 淘宝天猫 opencli 适配器

在新电脑上使用前，需要将此适配器复制到 opencli 用户目录：

**Windows:**
```
xcopy /y adapters\taobao\search.js %USERPROFILE%\.opencli\clis\taobao\search.js*
```

**Mac/Linux:**
```
mkdir -p ~/.opencli/clis/taobao
cp adapters/taobao/search.js ~/.opencli/clis/taobao/search.js
```
